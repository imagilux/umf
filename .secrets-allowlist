# Paths excluded from secret scanning (.githooks/scan-secrets.sh).
# Each line is a shell glob matched against the repo-relative path.
#
# Throwaway P-256/ed25519 keys generated solely for the sign/attest unit tests
# (see the generating `openssl genpkey` command documented above each const):
src/cli/sign/tests.rs
src/cli/attest/tests.rs
